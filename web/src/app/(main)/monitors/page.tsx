import type { Metadata } from "next";
export { default } from "./content";

export const metadata: Metadata = {
    title: "Monitors",
    description: "Track the uptime of your endpoints and get alerted when they go down.",
};
