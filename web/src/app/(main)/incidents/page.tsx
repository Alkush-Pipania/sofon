import type { Metadata } from "next";
export { default } from "./content";

export const metadata: Metadata = {
    title: "Incidents",
    description: "View all outages detected across your monitored endpoints.",
};
